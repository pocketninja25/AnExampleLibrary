# AnExampleLibrary

This is an example dotnet library for demonstration purposes.
The library provides some basic types, that can be instantiated, so that we can step into the code and test unity debugging.
